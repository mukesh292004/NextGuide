"use client";
import React from 'react'

const Button = () => {
  return (
    <div>
        <button className='btn btn-info' onClick={()=>{console.log("hi lolpandi")}}>Button</button>
    </div>
  )
}

export default Button